export const procesoPesado = ( iteraciones ) => {
    for (let i = 0; i < iteraciones; i++) {
        console.log(' Trabajando de pana de chill. ');            
    }
    console.log(' Termine de trabajar. ');
}